#!/bin/sh

echo -n "Push to server, no to pull(y,n)"; read input

[ $input = y ] && rsync -vrP --delete-after ~/website/ticklemaan root@ticklemaan.com:/var/www || rsync -vrP --delete-after root@ticklemaan.com:/var/www/ticklemaan ~/website
